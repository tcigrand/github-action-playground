name: CI2

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: echo Hello, world2!
    - name: Run a multi-line script
      run: |
        echo Add other actions to build2,
        echo test, and deploy your project2.
